<h1>タスク一覧</h1>

<p>ログインユーザー名: <%= current_user.name %></p>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path, class: "btn btn-primary" %>

